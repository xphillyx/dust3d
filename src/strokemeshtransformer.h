#ifndef DUST3D_STROKE_MESH_TRANSFORMER_H
#define DUST3D_STROKE_MESH_TRANSFORMER_H
#include <vector>
#include <QVector3D>

/*
class StrokeMeshTransformer
{
public:
    struct FillMesh
    {
        std::vector<Node> nodes;
        std::vector<QVector3D> vertices;
        std::vector<std::vector<size_t>> faces;
        std::vector<size_t> verticesSourceNodeIndices;
    };
    size_t addNode(const QVector3D &position, float radius);
    size_t addEdge(size_t firstNodeIndex, size_t secondNodeIndex);
    void transform();
};
*/

#endif
